def insertion_sort_chars(a):
    n = len(a)
    for i in range(1, n):
        key = a[i]
        j = i - 1
        while j >= 0 and a[j] > key:
            a[j + 1] = a[j]
            j -= 1
        a[j + 1] = key
    return a

print("Bài 7:", insertion_sort_chars(['d', 'a', 'c', 'b']))